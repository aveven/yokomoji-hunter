# 引き継ぎ指示書（2026-04-19）

## 対象プロジェクト
yokomoji-hunter: ~/Desktop/ClaudeProjects/yokomoji-hunter/

## 前セッションで完了したこと（push済み・EASデプロイ済み）

### クイズ解説カード UX 改善（app-mobile）
- **表示順を変更**: detail → value → usage から **value → usage → detail** に並び替え
  - 回答直後に「⭐ 覚えるとどうなる？」が最初に目に入るようになった
- **detail を折りたたみ式に変更**
  - デフォルト閉じ（「💡 詳しく ▼」）、タップで展開（「💡 詳しく ▲」）
  - 問題切り替え・タブ再フォーカス時に自動で閉じ直す
- `tsc --noEmit` エラーゼロ確認済み
- commit `e894277`・`npm run deploy:web:prod` 完了

## やること（この順番で）

### 1. 【最優先・持ち越し】`/handoff` 自動コミット動作確認
前々セッションから持ち越し中。`~/.claude/*` の編集制約がないセッションで実施すること。

**確認コマンド**:
```bash
cat ~/.claude/skills/handoff/SKILL.md | grep -A10 "自動コミット"
```

**期待動作**: `/handoff` 実行後、HANDOFF.md・SCRATCHPAD.md 生成 → history.json 更新 → ダッシュボード再生成 → 自動コミット＆プッシュ まで一連で完走すること。

### 2. （任意）実機で折りたたみ UX 確認
- モバイル版: https://yokomoji-hunter.expo.app
  - クイズタブ → 回答 → 解説カードで `💡 詳しく ▼` をタップして開閉できるか
  - 次の問題へ進んだとき `▼`（閉じ状態）にリセットされるか
- 問題なければそのまま。気になる点があれば調整

### 3. （任意）次フェーズ検討
現状のフェーズはすべて ✅ 完了。新機能を追加するなら候補:
- クイズ履歴（何問解いたか・正答率の推移）
- 苦手語ピックアップ（不正解が多い用語を優先出題）
- 用語検索（図鑑タブの検索機能強化）

## 注意事項
- app-mobile のデプロイ: `cd app-mobile && npm run deploy:web:prod`
- Web版（docs/）は push だけで GitHub Pages 自動反映
- `.vscode/` はコミット対象外

## 完了条件
- 【1】 `/handoff` が自動コミット＆プッシュまで完走することを確認
- 【2】 実機でクイズ解説カードの折りたたみが正常動作

## 参考
- detail を最後・折りたたみにした理由: 長文で画面を埋めると「次の問題へ」ボタンまで遠くなる。覚える価値（value）を最初に見せることで「なぜ覚えるか」が先に届く
- value を一番上にした理由: 正解/不正解直後に「それを覚えるとどうなるか」を見せることが定着率向上に直結する（メリット先出し）
